import { Analytics } from "@vercel/analytics/react";
import { space_grotesk, source_code_pro, inter } from "./../fonts.ts";
import "@code-hike/mdx/styles";
import "./../style.scss";

export default function App({ Component, pageProps }) {
  return (
    <main
      className={`${space_grotesk.variable} ${source_code_pro.variable} ${inter.variable}`}
    >
      <Component {...pageProps} />
      <Analytics />
    </main>
  );
}
